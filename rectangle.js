var length = 4;

var breadth = 2;

var area = length * breadth;

// var perimeter = length + length + breadth + breadth
var perimeter = 2 * (length + breadth)

console.log(area)
console.log(perimeter)